# Development tips

- After completing a task, commit the changes with a descriptive commit message.
- Format modified C/C++ code according to `.clang-format`.
- When requesting user input, do not set a timeout; wait indefinitely for a response.
